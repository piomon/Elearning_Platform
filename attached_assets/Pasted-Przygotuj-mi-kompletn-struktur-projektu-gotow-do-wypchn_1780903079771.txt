Przygotuj mi kompletną strukturę projektu gotową do wypchnięcia na GitHub, a następnie uruchomienia na VPS przez Docker.

Chcę, żeby projekt zawierał wszystko, co jest potrzebne do działania produkcyjnego środowiska:

Pełna struktura plików projektu
katalogi backendu,
katalogi frontendu, jeśli są potrzebne,
katalog konfiguracji,
katalog migracji bazy danych,
katalog skryptów deploymentowych,
pliki środowiskowe .env.example,
plik .gitignore,
dokumentację README.md.
Docker
Przygotuj komplet plików Docker:
Dockerfile dla backendu,
Dockerfile dla frontendu, jeśli projekt go wymaga,
docker-compose.yml,
osobny docker-compose.prod.yml, jeśli ma sens,
konfigurację sieci Docker,
wolumeny dla bazy danych,
healthchecki,
automatyczny restart kontenerów,
poprawne mapowanie portów,
konfigurację zmiennych środowiskowych.
Baza danych i migracje
Przygotuj wszystkie pliki związane z bazą:
konfigurację bazy danych,
folder migracji,
przykładowe migracje początkowe,
seedery, jeśli są potrzebne,
komendy do uruchomienia migracji,
instrukcję, jak wykonać migracje lokalnie i na VPS.
Deployment na VPS
Przygotuj instrukcję krok po kroku:
jak sklonować repozytorium z GitHuba na VPS,
jak utworzyć plik .env,
jak uruchomić projekt przez Docker Compose,
jak wykonać migracje,
jak sprawdzić logi,
jak zrestartować aplikację,
jak zaktualizować aplikację po kolejnym pushu na GitHub.
GitHub
Przygotuj:
instrukcję inicjalizacji repozytorium,
komendy git init,
git add,
git commit,
git remote add origin,
git push,
zasady, czego nie wrzucać do repozytorium,
poprawny .gitignore.
Konfiguracja produkcyjna
Uwzględnij:
bezpieczne zmienne środowiskowe,
oddzielenie konfiguracji lokalnej i produkcyjnej,
przykład .env.example,
brak wrzucania prawdziwych haseł do GitHuba,
restart kontenerów po awarii,
logowanie aplikacji,
podstawowe zabezpieczenia.
Reverse proxy / domena
Jeżeli jest potrzebne, przygotuj konfigurację pod:
Nginx albo Traefik,
domenę,
SSL przez Let’s Encrypt,
przekierowanie HTTP na HTTPS.
README
Wygeneruj kompletne README.md, które opisuje:
wymagania,
instalację lokalną,
uruchomienie przez Docker,
migracje,
deployment na VPS,
aktualizacje aplikacji,
najczęstsze problemy i ich rozwiązania.
Efekt końcowy
Chcę otrzymać:
listę wszystkich plików,
zawartość każdego pliku,
komendy do uruchomienia lokalnie,
komendy do wrzucenia na GitHub,
komendy do uruchomienia na VPS,
instrukcję migracji,
instrukcję aktualizacji aplikacji po zmianach.

Załóż, że projekt ma działać w pełni przez Dockera, bez ręcznego instalowania zależności aplikacji na VPS poza Dockerem i Docker Compose.